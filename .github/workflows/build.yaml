name: Build and Release Godot Project

permissions:
  contents: write

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        preset:
          - name: Windows Desktop
            file: 30-day-challenge.exe
          - name: Linux/X11
            file: 30-day-challenge.x86_64

    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Setup Godot 4
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.2.1
          include-templates: true

      - name: Export project
        run: |
          mkdir -p build/${{ matrix.preset.name }}
          godot --headless \
            --export-release "${{ matrix.preset.name }}" \
            build/${{ matrix.preset.name }}/${{ matrix.preset.file }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: 30-day-challenge-${{ matrix.preset.name }}
          path: build/${{ matrix.preset.name }}

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Zip builds
        run: |
          mkdir -p zips
          for dir in artifacts/*; do
            name=$(basename "$dir")
            zip -r "zips/${name}.zip" "$dir"
          done

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: zips/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
